/do while loop /
#include <stdio.h>

int main()
{

    int i=0,sum=0;
    do{
        printf("%d \n",i);
        i++;

        sum+=i;

        }while (i<=50);

   printf("The sum  is %d",sum);

return 0;
}

